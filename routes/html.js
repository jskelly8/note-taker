const htmlRoute = require('express').Router();
const path = require('path');

// GET request responding with index.js

// GET request responding with notes.html

// -------------------------------------
module.exports = htmlRoute;